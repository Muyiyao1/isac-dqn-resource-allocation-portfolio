# Notebooks

No notebook is required for the core training or evaluation pipeline.

Use this directory for exploratory analysis notebooks if needed. Keep reproducible experiment logic in `src/` and `run_experiment.py`.
