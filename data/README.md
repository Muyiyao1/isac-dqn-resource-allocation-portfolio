# Data

The current experiments generate simulation states procedurally from `configs/config.yaml`. No external dataset is required.

Use this directory for future fixed scenario files, channel traces, or exported input datasets.
